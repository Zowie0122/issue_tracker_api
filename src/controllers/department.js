const ERRS = require("../utils/errors");

/**
 * list all the departments by company id
 * @param {*} req
 * @param {*} res
 * @param {*} next
 */
const list = (req, res, next) => {};

module.exports = { list };
